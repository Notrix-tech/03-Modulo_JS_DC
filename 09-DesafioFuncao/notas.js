//
let botao = document.getElementById("exc")
botao.addEventListener("click", function(evento){
    evento.preventDefault()
    documento.write(" A médoa é " + mediaPonderada(Number(n1.value),Number(n2.value), Number(n3.value)))
})